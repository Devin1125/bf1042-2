# 偉大的早餐店系統架構

## 一、系統概述

本系統是「Devin的早餐店」的預約點餐平台，目標是讓顧客可以在營業時間前或營業時間內完成早餐預約，並讓櫃台、廚房、店長與管理者用不同角色處理訂單。系統採用三層式架構，將畫面、商業邏輯與資料儲存分開，讓功能更容易維護，也方便未來擴充。

三層式架構包含：

1. 表現層：負責使用者看到與操作的畫面。
2. 應用層：負責 API、登入驗證、訂單規則與角色權限。
3. 資料層：負責資料庫、資料表與訂單資料保存。

## 二、第一層：表現層

表現層是使用者直接操作的前端介面，主要位於 `frontend/src`。顧客、櫃台、廚房、店長與管理者都會透過瀏覽器使用系統。

### 主要職責

- 顯示早餐店菜單與分類。
- 提供購物車功能。
- 讓顧客使用 `+` / `-` 調整餐點份數。
- 讓顧客填寫每個餐點的客製化需求。
- 讓顧客選擇預約取餐時間。
- 顯示訂單歷史與訂單狀態。
- 依照角色顯示不同工作台。

### 對應功能

- 顧客看到菜單、購物車、預約時間、訂單歷史。
- 櫃台看到已送出的訂單與取餐時間。
- 廚房看到需要製作的餐點與客製化需求。
- 店長與管理者看到營運資料、菜單管理與角色管理。

## 三、第二層：應用層

應用層是系統的核心，主要位於 `backend.ts`、`store`、`shared` 與驗證 schema。這一層負責接收前端請求、檢查資料是否合法、套用商業規則，再把資料交給資料層處理。

### 主要職責

- 提供 REST API。
- 處理登入狀態與使用者 session。
- 檢查角色權限。
- 驗證菜單、訂單與角色申請資料。
- 建立購物車與更新購物車品項。
- 儲存每個品項的客製化備註。
- 限制預約時間必須在營業時間 `06:00` 到 `10:00`。
- 控制訂單狀態流程，例如待處理、製作中、可取餐、已完成。

### 重要規則

- 未登入使用者不能送出訂單。
- 顧客只能查看自己的訂單。
- 櫃台只處理已送出的訂單。
- 廚房只處理需要製作的訂單。
- 店長與管理者可以查看更完整的營運資料。
- 預約取餐時間不能早於現在，也不能超出營業時間。

## 四、第三層：資料層

資料層負責保存系統資料，正式版本使用 Neon PostgreSQL，schema 為 `bf_v10`。資料表由 Drizzle schema 管理，讓 TypeScript 程式碼與資料庫結構保持一致。

### 主要資料

- 使用者資料。
- 角色與權限資料。
- 菜單品項。
- 購物車與訂單。
- 訂單品項。
- 每個訂單品項的客製化備註。
- 角色申請與審核紀錄。

### 重要資料表

| 資料表 | 作用 |
| --- | --- |
| `menu_items` | 儲存早餐店菜單，例如紅茶、奶茶、蛋餅、早餐吃到飽 |
| `orders` | 儲存訂單主資料，例如使用者、總金額、狀態、預約取餐時間 |
| `order_items` | 儲存訂單中的每個餐點、數量、價格與客製化備註 |
| `role_requests` | 儲存使用者申請成為櫃台或廚房角色的資料 |
| `users` | 儲存登入使用者資料 |

## 五、三層架構流程

顧客送出一份早餐訂單時，資料會依照下列流程移動：

1. 顧客在表現層選擇餐點、調整數量、填寫客製化需求與預約時間。
2. 表現層把資料送到應用層 API。
3. 應用層驗證使用者是否登入、訂單內容是否合法、預約時間是否在營業時間內。
4. 驗證通過後，應用層把訂單與品項資料寫入資料層。
5. 資料層保存訂單、數量、總金額、取餐時間與客製化備註。
6. 櫃台與廚房重新讀取訂單時，會從資料層取回最新資料並顯示在表現層。

## 六、三層架構優點

- 畫面與後端規則分離，前端改版時不需要重寫資料庫。
- 商業規則集中在應用層，比較容易維護。
- 資料層集中管理，方便備份、部署與查詢。
- 角色權限清楚，顧客、櫃台、廚房、店長與管理者看到的資料不同。
- 未來可以擴充付款、庫存、優惠券、通知與報表功能。

## 七、系統架構總結

「Devin的早餐店」使用三層式架構，把使用者介面、系統邏輯與資料庫分開。表現層負責讓使用者操作，應用層負責驗證與商業規則，資料層負責保存資料。這樣的架構能讓早餐店系統更穩定、更容易維護，也更適合未來繼續加入新功能。
